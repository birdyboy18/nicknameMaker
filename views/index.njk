<html>
<head>
	<meta charset="UTF-8">
	<title>Bystrik Random Name Generator</title>
	<link href='https://fonts.googleapis.com/css?family=Quando|Montserrat:400,700' rel='stylesheet' type='text/css'>
	<style>

		* {
			box-sizing: border-box;
		}

		html, body {
			height: 100%;
			font-family: "Montserrat", sans-serif;
		}

		body {
			background: #EACB35;
			line-height: 1.5;
		}

		.nickname {
			top:50%;
			left:50%;
			transform: translate(-50%, -50%);
			position:absolute;
			width: 60%;
		}

		.nickname h1 {
			font-family: "Quando", sans-serif;
			font-size: 64px;
			text-transform: capitalize;
			color: #113333;
		}

		.site-title {
			position: absolute;
			top: 10px;
			left:10px;
			padding: 8px;
			background: rgba(0,0,0,0.15);
			display: block;
			color: #EACB35;
			font-family: "Montserrat", serif;
			font-size: 13px;
		}

		.message {
			margin-top: 80px;
			display:block;
			color: rgba(0,0,0,0.6);
		}

		.examples {
			padding: 50px 0 20px 0;

		}

		.example {
			display: inline-block;
			padding: 10px 20px 10px 0px;
			width: 33.3333333%;
			vertical-align: top;
			color: rgba(0,0,0,0.6);
		}

		.example strong {
			font-weight: 700;
			color: rgba(0,0,0,0.8);
			text-transform: capitalize;
		}

	</style>
</head>
<body>
	<div class="site-title">
		<span>The Official Bystrik Ondica Nickname Generator</span>
	</div>
	<div class="nickname">
		<h1>{{ nickname }}</h1>
		<span class="message">Why don't you try it out? Say some of the following sentences to get a feel for it:</span>
		<div class="examples">
			<span class="example">Oi, <strong>{{ nickname }}</strong> Hows it Hangin'?</span><span class="example">God Dam it <strong>{{ nickname }}</strong>!</span><span class="example">Hey <strong>{{ nickname }}</strong>, How's the Wife?</span>
		</div>
	</div>
</body>
</html>
